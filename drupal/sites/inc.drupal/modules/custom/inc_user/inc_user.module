<?php

use Drupal\Core\Form\FormStateInterface;

function inc_user_user_insert($account) {
  // dump($account); die;
}

function inc_user_form_alter(array &$form, FormStateInterface $form_state) {
  $form['account']['mail']['#required'] = TRUE;
}
